return {
  summary = "A test bench for Gen151: force its spawns, hear its sounds, flip its gates.",
  author = "Wild",
  tags = { "tool", "developer", "companion" },
  differences = {
    changed = {
      "Nothing, while every row is left alone -- the bench only acts when a "
        .. "row is chosen",
    },
    added = {
      "A GEN151 BENCH row on the OPTIONS menu, and the screen behind it",
    },
    known = {
      "This is a development tool and is meant to be deleted before release; "
        .. "it is a separate mod so that deleting it is all it takes",
      "Forcing spawns changes WHAT you meet, never whether you meet it, so "
        .. "it cannot be used to judge encounter rate",
      "GO TO warps to cell 5,5, which on a few maps is a wall -- walk out or "
        .. "warp again",
    },
  },
  credits = {
    { who = "Wild", for_ = "Gen151, whose placement table this reads" },
  },
  compat = { engine = ">=1.0.0 <2.0.0", modApi = 2 },
}
